export interface FooterProps {
  footerTitle: string;
  footerLogo: imageProps[];
  footerSocialMediaImage: imageProps[];
  footerCopyrightNotice: string;
}

export interface imageProps {
  name: string;
  url: string;
}
