export interface HeaderProps {
  headerTitle: string;
  headerLogo: imageProps[];
  headerOptions: string[];
  heroPrincipalTitle: string;
  heroSlogan: string;
  heroGifBackground: imageProps[];
}

export interface imageProps {
  name: string;
  url: string;
}
